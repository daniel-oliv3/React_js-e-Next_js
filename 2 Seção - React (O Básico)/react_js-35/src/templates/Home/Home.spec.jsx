describe('<Home/> ', () => {  
  it('is a dummy test', () => {
    expect(1).toBe(1);
  });

  it('test two', () => {
    expect(1).toBe(1);
  });

  it('test three', () => {
    expect(1).toBe(1);
  });
  
});
